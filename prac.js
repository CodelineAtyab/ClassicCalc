//multiply method by aladhary
const calculator = {
    multiply: function(a, b) {
      return a * b;
    },
  };
  
  // Test the method
  const num1 = 4;
  const num2 = 6;
  const result = calculator.multiply(num1, num2);
  console.log(`${num1} * ${num2} = ${result}`);