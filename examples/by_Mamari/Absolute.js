function absoluteValue(number) {
    if (number < 0) {
      return -number;
    } else {
      return number;
    }
  }
  
  let number = -5;
  let value = absoluteValue(number);
  
  console.log(absoluteValue);